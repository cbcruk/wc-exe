# AGENTS.md — wc-exe 불변식과 그 근거

이 문서는 아키텍처 설명서가 아니라 **판정 기준**이다. 여기 적힌 것은 이 저장소에서
이미 한 번씩 깨져 본 규칙이고, 깨졌을 때 무엇을 잃었는지가 같이 적혀 있다. 코드
리뷰에서 "왜 이렇게 해야 하죠"가 나오면 답은 여기 있어야 한다. 여기 없으면 그건
불변식이 아니라 취향이다.

구조 설명과 실측 기록은 `docs/persistent-runner.md`, `docs/vfs-interface.md`,
`docs/virtual-filesystem.md`에 있다. 이 문서는 그 위에 얹는 짧은 목록이다.

---

## 시스템을 한 문장으로

wc-exe는 **사용자의 브라우저 탭에서 도는 WebContainer를 Node에서 원격으로 운전하는
브릿지**이고, CLI와 데몬은 그 브릿지의 소비자다. 호스트는 컨테이너를 소유하지 않는다 —
탭이 소유한다. 이 한 문장에서 아래 불변식 대부분이 따라 나온다.

```
src/core/      브릿지. CLI를 모른다.
src/commands/  build · dev · install. 브릿지의 한 소비자.
src/daemon/    부팅된 컨테이너를 살려 두는 또 다른 소비자.
src/runner/    페이지 번들. 호스트와 코드를 공유하지 않는다.
```

---

## 외울 불변식

### 경계

**1. `core`는 CLI에 의존하지 않는다.**
`commander`·`ora`·`chalk`·`chokidar`·`http-proxy` 금지, `commands`/`daemon`/`utils`로의
역참조 금지. 브릿지는 이 CLI보다 오래 살 물건이고, 의존성 하나가 조용히 그걸 하나의
CLI에 용접한다. 빌드가 통과하는 것으로는 아무것도 알 수 없다.
→ `src/core/boundary.test.ts`가 강제한다. **아무도 확인하지 않는 경계는 경계가 아니다.**

**2. 호스트와 러너는 코드가 아니라 wire 포맷만 공유한다.**
별개 번들이라 공유가 불가능하다. `core/errors.ts`와 `runner/src/errors.ts`가 서로를
import하지 않는 것은 실수가 아니라 이 제약이다. 공유하는 것은 `{ _tag, message, ...fields }`
평범한 객체 하나뿐 — 라이브러리가 복원해야 하는 형태가 아니어야 한다.

**3. 사용자에게 보일 문장은 한 곳에서만 조립한다.**
`CommandFailed`의 문장은 호스트의 `commandFailure()`가 필드에서 만든다. 러너는 필드만
싣는다. 양쪽에서 조립하면 어느 한쪽이 문구를 바꾸는 순간 두 벌이 갈린다.

### 실패

**4. 실패는 문자열이 아니라 태그다. 단, 분기하는 호출자가 있을 때만.**
태그 추가 기준은 "다른 실패인가"가 **아니라** "어떤 호출자가 이걸로 분기하는가"다.
현재 분기는 두 개뿐이다 — `runtimeStateIsKnown()`과 `exitCodeFor()`. 둘 중 어디에도
안 나타나고 다른 `match`에도 없는 태그는 문자열에 절차만 붙인 것이다.

**5. 런타임 재사용은 allowlist로만 허용한다.**
`RUNTIME_STATE_KNOWN`에 없는 태그는 기본이 "재사용 불가"다. 방향이 핵심이다:
보수적으로 틀리면 부팅 한 번을 더 하고, 반대로 틀리면 **아무도 상태를 설명할 수 없는
컨테이너에서 나온 산출물을 출하한다.** 후자는 에러가 아니라 성공처럼 보인다.
(`UploadFailed`가 목록에 없는 이유는 `core/errors.ts`에 적혀 있다. 논증이 되는 것과
보수적인 allowlist의 기준을 넘는 것은 다르다.)

**6. 종료 코드가 실패의 종류를 말한다.**
`1` 프로젝트가 빌드되지 않았다 · `2` 호출이 틀렸다 · `3` wc-exe가 제 일을 못 했다.
전부 `1`이던 시절 CI에서 "누가 봐야 할 빨간 빌드"와 "재시도하면 되는 인프라 문제"가
구분되지 않았다. `reportFailure()`를 거치지 않고 종료하는 경로를 만들지 않는다.

**7. 모르는 태그는 삼키지 않고 `UnknownFailure`로 원래 태그를 보존한다.**
호스트와 페이지 번들은 버전이 어긋날 수 있다(사용자가 열어 둔 옛 탭). 어긋남이
"혼란"이 아니라 **자기 모습 그대로** 드러나야 한다.

**8. 호출자에게 도달하지 않는 async도 보고된다.**
타입이 붙은 에러는 _기다리는 호출자가 있는_ 실패에만 닿는다. 나머지 — 아무도 await하지
않은 promise, 반환형이 `void`인 콜백 안의 rejection — 는 타입 시스템 밖이다.
빌드 타임에 `no-floating-promises` + `no-misused-promises`(type-aware, `src`만),
런타임에 `reportUnhandled()`. 데몬은 `exitOnRejection: false` — 남의 세션을 들고 있는
프로세스가 rejection 하나로 죽으면 안 된다.

**9. 종료 경로는 실패해도 끝까지 간다.**
정리 실패는 이름을 붙여 보고하되 전파하지 않는다. `.then`이 아니라 `.finally`로 exit한다.
한 세션의 `close()`가 reject해서 `clearRecord()`와 `process.exit(0)`이 통째로
건너뛰어진 적이 있고, 증상은 "`daemon stop`이 아무 일도 안 하는 것"이었다.

### 상태

**10. 세션은 빌드 결과를 캐시하지 않는다.**
비싼데 프로젝트와 무관한 것(부팅된 런타임, `node_modules`)만 캐시하고, 프로젝트에
종속된 것은 매번 다시 유도한다. 여기서 맞바꾸는 반대편은 느린 빌드가 아니라 **틀린
빌드**다.

**11. 삭제는 전파된다.**
호스트에서 사라진 경로는 런타임에서도 지운다. mount는 추가·덮어쓰기만 하므로, 지우지
않으면 오래 사는 컨테이너 안에서 그 파일이 계속 resolve되고 **존재하지 않는 소스로
빌드가 계속 성공한다.** 여기서 가능한 최악의 결과다 — 성공처럼 보이니까.

**12. 캐시 오리진은 고정이다.**
OPFS는 origin(scheme+host+port) 스코프다. `CACHE_PORT`가 5199로 고정된 것은 그
이유고, `localhost`와 `127.0.0.1`은 **다른 오리진**이다(캐시가 둘로 갈렸던 적이 있다,
`docs/persistent-runner.md` §13.4). 오리진을 바꾸는 변경은 캐시를 버리는 변경이다.

**13. 빌드 시퀀스는 하나다.**
`runProjectBuild()`. 호출자마다 정당하게 다른 것(`mount`, `prepareOutput`)은 주입하고,
분기하지 않는다. 두 벌이던 시절 일회성 경로는 핀 고정된 매니저로, 데몬은 런타임이
품고 있는 버전으로 빌드했다. 양쪽 다 성공했고 산출물만 달랐다.

### 안전과 진단

**14. 제어 평면은 두 겹으로 막는다.**
`Origin` 헤더가 있으면 거부(웹 페이지가 보낸 것이다) + 상수시간 토큰 비교. 데몬은
고정 포트에서 계속 살아 있으므로 일회성 서버보다 훨씬 큰 표적이다. 토큰 파일은 `0600`,
temp 파일에 쓰고 rename, 모드를 **처음부터** 걸고 만든다(나중에 chmod하면 읽히는 창이 생긴다).

**15. 추측한 경로로 늦게 실패하지 않는다.**
`resolveRunnerDist()`는 후보를 확인하고, 없으면 **시작 시점에** 시도한 경로를 전부
말하며 던진다. 고정된 `../` 개수로 풀던 시절 데몬은 모든 애셋에 404를 조용히 내주고
페이지는 이유 없이 타임아웃했다.

**16. 테스트 더블은 캐스트가 아니라 좁힌 인터페이스로 만든다.**
`BuildRunner`, `SessionRunner`가 그것이다. `as unknown as` 가짜는 인터페이스가 어떻게
움직이든 계속 컴파일된다 — **드리프트가 위험해지는 바로 그 순간에 드리프트를 못 본다.**

### 운영

**17. 백그라운드 작업은 수렴 루프다.**
매 패스마다 (설정, 지금의 사실)에서 원하는 상태를 계산하고, 가장 중요한 차이 **하나만** 크기를
제한해서 메운다. "이벤트가 오면 X를 한다"로 짜면 이벤트를 한 번 놓쳤을 때 영구적인 구멍이
남는다 — 실제로 남아 있었다: 아무것도 세션을 회수하지 않아서, poisoned 세션은 같은 프로젝트를
누군가 다시 빌드할 때까지 링크를 쥐고 있었고 `lastUsedAt`은 "for idle eviction"이라는 주석을
단 채 아무 데도 쓰이지 않았다. 판단은 순수 함수라 데몬도 타이머도 없이 테스트된다.
→ `src/daemon/maintenance.ts`

**18. 정확한 것만으로는 부족하다 — 왕복 횟수가 예산이다.**
프로토콜을 바꾸는 변경은 왕복 횟수로 심사한다. 제어 채널 호출 1회가 러너 평면 요청 N개로
펼쳐지므로 **둘을 따로 센다.** 핫 패스의 비용은 프로젝트 크기가 아니라 diff 크기에 비례해야
한다. 벽시계 시간은 여기서 다루지 않는다 — 그건 `bench/`이고 실제 머신이 필요하다.
→ `docs/ROUNDTRIPS.md`

---

## 무엇이 이걸 지키는가

규약으로 남겨 둔 것은 잊힌다. 현재 강제되는 것과 아직 안 되는 것을 구분해 둔다.

| 불변식                  | 강제 장치                                                                          |
| ----------------------- | ---------------------------------------------------------------------------------- |
| 1 core 경계             | `src/core/boundary.test.ts` (import 스캔)                                          |
| 6 종료 코드             | `exitCodeFor()` + `reportFailure()` 단일 경로                                      |
| 8 떠도는 async          | eslint type-aware 2종 + `reportUnhandled()`                                        |
| 5 재사용 allowlist      | `RUNTIME_STATE_KNOWN` — 기본값이 안전한 쪽                                         |
| 14 제어 평면            | `controlPlaneGuard` + `src/daemon/auth.test.ts`                                    |
| 2·3·7 wire 계약         | `src/core/errors.test.ts` — round-trip, 그리고 어휘의 **모든** 태그를 도는 테스트  |
| 11 삭제 전파            | `src/daemon/daemon.integration.test.ts` — 인메모리 런타임에 실제로 반영되는 것까지 |
| 17 수렴 루프            | `src/daemon/maintenance.test.ts` — 판단이 순수 함수라 직접 테스트된다              |
| 18 왕복 예산            | `daemon.integration.test.ts`의 `what one build costs` — 정확한 값으로 고정         |
| **4 태그 추가 기준**    | **없음 — 리뷰가 본다**                                                             |
| **13 단일 빌드 시퀀스** | **타입이 막아 주지만 새 경로를 새로 짜는 것은 못 막는다**                          |

굵게 표시된 두 줄이 다음에 자동화할 후보다.

### 테스트 계층

느린 의존성 하나(브라우저 안의 WebContainer)를 인터페이스 뒤로 밀어 넣으니 계층이 생겼다.
`FakeRunnerPage`는 그 인터페이스 — 제어 채널 — 의 두 번째 구현이고, 그 위는 전부 진짜다:
진짜 소켓, `RunnerLink`, `RunnerClient`, `runProjectBuild`, `Session`, 데몬 라우트.

| 계층                 | 명령                     | 필요한 것          | 걸리는 시간 |
| -------------------- | ------------------------ | ------------------ | ----------- |
| 단위 + 인메모리 통합 | `pnpm test`              | 없음               | ~10초       |
| 실제 브라우저 e2e    | `test/integration/*.mjs` | 빌드 + 데스크톱 탭 | 분 단위     |
| 성능                 | `bench/*.mjs`            | 위와 같음          | 분 단위     |

`pnpm test`는 **아무것도 빌드하지 않고** 돈다. 러너 번들이 없어도 되는 이유는
`createApp`/`startDaemon`의 `runnerDist: null` — 페이지를 로드하지 않는 러너에게는
서빙할 페이지가 필요 없기 때문이다. (이전에는 `pnpm build:runner` 없이는 통째로 실패했다.)

인메모리 계층이 증명하는 것은 **배관**이다: 호출 순서, 파일이 런타임에 들어가고 산출물이
나오는 것, 삭제가 반영되는 것, 실패가 태그를 유지한 채 두 transport를 건너는 것.
**실제 프로젝트가 빌드되는지는 증명하지 않는다** — 명령이 실제로 돌지 않는다.

## 바꿀 때

- **새 실패 태그를 추가한다면**: 어떤 호출자가 이걸로 분기하는지 먼저 적는다. 답이 없으면
  기존 태그의 필드로 충분한지 다시 본다. 추가했다면 `TAGS`, `WcError`, `fromWire`의
  `switch`, 그리고 재사용 가능 여부 판단까지 같이 간다 — `RUNTIME_STATE_KNOWN`에
  **넣지 않는 것도 결정**이고, 기본값이 안전한 쪽이므로 넣지 않으면 그냥 동작한다.
- **`core`에 의존성을 추가한다면**: 그게 터미널을 전제하는지 본다. 전제한다면 소비자 쪽이다.
- **오리진·포트를 건드린다면**: 캐시를 버리는 변경이다. 그럴 만한지 적는다.
- **프로토콜을 바꾼다면**: 왕복이 늘어나는지 본다. 늘어나면 `docs/ROUNDTRIPS.md`에 왜 그만한
  값어치가 있는지 적고, `what one build costs`의 숫자를 같이 고친다. **`toBeLessThan`으로
  바꾸지 않는다** — 상한만 있는 예산은 한 번에 하나씩 올라가면서 의미를 잃는다.
- **백그라운드 작업을 추가한다면**: 타이머 콜백 안의 `if`가 아니라 `nextMaintenanceAction`의
  우선순위 목록에 넣는다. 왜 그 순위인지 한 줄 적는다.
- **성능 주장을 한다면**: `bench/`로 재고 숫자를 남긴다. 통과하는 테스트를 커버리지로
  읽지 않는다 (아래).

## 검증되지 않은 것 — 통과를 과신하지 말 것

정직하게 적어 둔다. 이 목록이 비어 보이는 것보다 낫다.

- **`clearDist`가 실제로 필요한지 증명된 적 없다.** 빼도 parity 테스트는 통과한다 —
  vite가 자기 `outDir`를 스스로 비우기 때문이다. 그러지 않는 빌드 도구를 위해 남겨
  뒀지만, **통과하는 테스트를 이것의 근거로 읽으면 안 된다.**
- **호스트/러너 버전 스큐(7)를 실제로 재현하는 테스트가 없다.** 경로는 있고 수동으로만 확인했다.

- **두 번째 빌드도 파일을 전부 다시 읽는다.** 알려진 비용 위반이고, 고치려면 러너 번들의
  API를 바꿔야 한다. 숫자는 테스트에 박아 뒀다 — `docs/ROUNDTRIPS.md`.

## 참고

이 목록의 형태는 [tobi/walgit](https://github.com/tobi/walgit)의 "Invariants worth
memorising"에서 가져왔다. 위의 테스트 계층도 walgit의 `ObjectStore` + memory 백엔드에서
가져온 것이다 — 느린 의존성 하나를 인터페이스 뒤로 밀면 계층은 저절로 생긴다.

**수렴 루프**(불변식 17)와 **비용 모델**(불변식 18, `docs/ROUNDTRIPS.md`)도 거기서 왔다.
비용 모델 쪽은 가져오면서 실제 결함이 하나 나왔다 — 두 번째 빌드가 파일을 전부 다시 읽는다는 것,
그리고 그렇지 않다고 적혀 있던 주석.
